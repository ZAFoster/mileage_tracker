<h1><%= @title %></h1>

<table class="table">
  <tr>
    <th>Name</th>
  </tr>

<% @makes.each do |make| %>
  <tr>
    <td><%= make.name %></td>
    <td><%= link_to 'Edit', edit_make_path(make) %></td>
    <td><%= link_to 'Delete', make, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>


</table>

<br />

<%= link_to 'New Make', new_make_path %>
